function getRandomInt(min, max){
    min = Math.ceil(min);
    max = Math.floor(max);

    return Math.floor(Math.random() * (max-min)) + min;
}

async function test(){
    let counter = 0;

    for(let i=0 ; i<5 ; i++){
        const sec = getRandomInt
    }
}

